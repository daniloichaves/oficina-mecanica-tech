apiVersion: v1
kind: Service
metadata:
  name: oficina-app
  namespace: oficina
spec:
  type: ClusterIP
  selector:
    app: oficina-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
